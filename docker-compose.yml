version: '3.4'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: P4$$W0rd3nCrypt3d

  app:
    image: python3
    restart: always
    ports:
      - 5000:5000